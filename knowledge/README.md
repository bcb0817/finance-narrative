# Local learning data

Runtime learning notes are intentionally excluded from Git. The empty category
directories are retained with `.gitkeep` files so a fresh checkout has the
expected structure without publishing operational learning data.
